<div class="musiki-pod" data-pod="strudel" data-pod-title="STRUDEL">
  <section class="conference-stage-panel conference-stage-panel--strudel" data-panel="strudel">
    <div class="strudel-pod-host" data-strudel-host>
      <div class="strudel-pod-status" data-strudel-status>STRUDEL READY</div>
    </div>
    <div class="strudel-pod-console" data-strudel-console data-level="ready" role="status" aria-live="polite">
      ready · cmd/ctrl+enter play · cmd/ctrl+. stop
    </div>
  </section>
</div>

<style is:global>
  .conference-stage-panel--strudel,
  .strudel-pod-host {
    width: 100%;
    height: 100%;
    min-height: 0;
  }

  .conference-stage-panel--strudel {
    display: flex;
    flex-direction: column;
    padding: 0;
    overflow: hidden;
    background: var(--c-bg, #000);
  }

  .strudel-pod-host {
    position: relative;
    display: flex;
    flex-direction: column;
    flex: 1;
    height: auto;
    overflow: hidden;
    color: var(--c-fg, #fff);
  }

  .strudel-pod-host > strudel-editor {
    display: none;
  }

  .strudel-pod-host > div:not(.strudel-pod-status) {
    position: relative;
    z-index: 1;
    flex: 1;
    min-height: 0;
    overflow: hidden;
    background: transparent !important;
  }

  .strudel-pod-host > #hydra-canvas {
    position: absolute !important;
    inset: 0 !important;
    z-index: 0 !important;
    width: 100% !important;
    height: 100% !important;
    pointer-events: none !important;
  }

  .strudel-pod-host .cm-editor,
  .strudel-pod-host .cm-scroller {
    height: 100%;
    min-height: 0;
  }

  .strudel-pod-host .cm-editor {
    background: transparent !important;
  }

  .strudel-pod-host .cm-scroller {
    overflow: auto;
    font-family: "DankMonoNerdFontMono", ui-monospace, monospace !important;
  }

  .strudel-pod-status {
    position: absolute;
    inset: 50% auto auto 50%;
    z-index: 2;
    transform: translate(-50%, -50%);
    font-size: 0.55rem;
    letter-spacing: 0.14em;
    opacity: 0.45;
    pointer-events: none;
  }

  .strudel-pod-host[data-ready="true"] .strudel-pod-status {
    display: none;
  }

  .strudel-pod-console {
    box-sizing: border-box;
    flex: 0 0 2.45em;
    width: 100%;
    height: 2.45em;
    min-height: 2.45em;
    max-height: 2.45em;
    padding: 0.22em 0.75em;
    overflow: hidden;
    border-top: 1px solid rgba(245, 158, 11, 0.55);
    background: linear-gradient(180deg, rgba(120, 53, 15, 0.26), rgba(69, 26, 3, 0.42));
    color: #fbbf24;
    font-family: "DankMonoNerdFontMono", ui-monospace, monospace;
    font-size: 0.48rem;
    line-height: 1.12;
    white-space: pre-wrap;
    overflow-wrap: anywhere;
  }

  .strudel-pod-console[data-level="error"] {
    border-top-color: rgba(251, 146, 60, 0.9);
    background: linear-gradient(180deg, rgba(146, 64, 14, 0.38), rgba(67, 20, 7, 0.62));
    color: #fed7aa;
  }

  .pod-diy-btn--strudel-transport {
    font-size: 9px !important;
    opacity: 0.8;
  }

  .pod-diy-btn--strudel-transport[data-playing="true"] {
    color: var(--pod-color, #f6b26b) !important;
    opacity: 1;
  }
</style>
