// Thin re-export: see annotations.ts for why access enforcement doesn't
// need to be repeated here.
export { GET, POST, PUT, DELETE } from '../../live/notes/trace';

export const prerender = false;
