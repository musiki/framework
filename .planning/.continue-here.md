---
context: default
phase: ad-hoc-regression-fixes
task: 11
total_tasks: 13
status: in_progress
last_updated: 2026-05-24T05:16:38.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Out-of-scope additions break Vite dep optimization | CoursePodWorkspace imported RoomWorkspaceManager+SA+SV — not in optimizeDeps.include. Every restart re-hashed all chunks including codemirror, producing persistent 504s | advisory | Always check if new heavy imports are in `optimizeDeps.include` in astro.config.mjs when adding room-workspace deps to course pages |
| Clearing .vite cache while server is running | Deleting node_modules/.vite while dev server is up causes partial re-optimization with inconsistent chunk hashes | advisory | Always stop server first, then clear .vite, then restart |

<current_state>
Session ended after production deploy. NOTAS (DB notes) confirmed working on dev. Dockview pods restored. Production deployed + DB migrated + CONTENT_ADMIN_LOCAL_WRITE=true added to VPS .env. Remaining: verify .md filesystem notes open correctly in dockview on production.
</current_state>

<completed_work>

- Task 1–6: All original regressions fixed (dockview courseId, CONCEPTS grouping, search icon, camera icon, note-folders userId, openNote 404) — committed 6bb1730 + 374837e
- Task 7: Removed out-of-scope SA/SV/HP (CoursePodWorkspace) from course page — root cause of persistent Vite 504 errors — committed c2faeb4
- Task 8: Deployed to VPS production (git pull + npm run build + docker restart devmusiki-app)
- Task 9: Applied LiveClassNoteFolder userId migration on VPS production DB
- Task 10: Added CONTENT_ADMIN_LOCAL_WRITE=true to /opt/musiki/framework/.env on VPS + restarted
</completed_work>

<remaining_work>

- Task 11: Verify .md notes open in dockview on production — click a lesson in sidebar, confirm no 503/404
- Task 12: If .md notes still fail — investigate slug→filename resolution in getCourseNote (Obsidian filenames use spaces/accents; check if lessonPathIndex stores real path or slug)
- Task 13: NOTAS pod — create/edit notes in dockview (unblocked now that dockview works)
</remaining_work>

<decisions_made>

- bootWorkspace reads courseId from [data-course-layout-root] data-live-course-id (not removed [data-notes-sidebar])
- Content-collection sidebar entries navigate normally — /api/notes/get only serves filesystem files
- Strip courseId prefix before ws.openNote (data-lesson-page-slug = courseId/path, getCourseNote expects bare path)
- CoursePodWorkspace removed entirely — was never in the plans, caused Vite 504 cascade
- HP ribbon button → disabled/inactive (per original ribbon-nav.md spec)
- CONTENT_ADMIN_LOCAL_WRITE=true added to VPS production .env
</decisions_made>

<blockers>
No current blockers.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full task list and decisions
2. Source of truth plans:
   - `…/docs/superpowers/plans/2026-05-23-notas-workspace-qa-pod.md`
   - `…/docs/superpowers/plans/2026-05-22-ribbon-nav.md`

## Infrastructure State
- VPS production: deployed c2faeb4, DB migrated, CONTENT_ADMIN_LOCAL_WRITE=true, devmusiki-app running
- Dev server: stopped
- Vite dep cache: stable (heavy room-workspace imports removed from course page)

## Context on .md notes filesystem resolution

`/api/notes/get` calls `getCourseNote(courseId, slug)` in `src/lib/notes-fs.ts`.
- `notesPreflightError` now passes on production (CONTENT_ADMIN_LOCAL_WRITE=true fixed)
- Local files use Obsidian naming: spaces + accents (e.g. `"Metodología de la Investigación en Música MOC.md"`)
- The slug passed from `lessonPathIndex.pathByEntryId` — need to verify if this stores the real filename path or a slugified version
- If slug = `"10-mim/Metodología de la Investigación en Música MOC"` → file found ✓
- If slug = `"10-mim/metodologia-de-la-investigacion-en-musica"` → file NOT found ✗

<context>
Good session — main regressions resolved, production stable. SA/SV/HP removal was the key insight: those imports caused Vite to perpetually re-optimize chunks, explaining why clearing .vite appeared not to work. NOTAS (DB notes) confirmed working by user.
</context>

<next_action>
Open musiki.org.ar as teacher, navigate to s123 course, click a lesson in sidebar. Check browser console — should see [dockview] logs and note should load in dockview panel. If 503/404 persists, add console.log at the ws.openNote call in slug.astro (~line 4445) to capture the exact slug value, then compare to actual filenames on disk.
</next_action>
