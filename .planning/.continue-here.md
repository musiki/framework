---
context: default
status: in_progress
last_updated: 2026-05-24T04:40:20.070Z
---

# BLOCKING CONSTRAINTS

- [ ] CONSTRAINT: dockview-still-lost — bootWorkspace code is correct but dockview still doesn't render. Identify the exact failure point via console.log diagnostics BEFORE touching anything else.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|-----------|
| Fix-without-verifying | Multiple fixes applied without confirming each worked | advisory | Verify dockview renders before touching other features |

<current_state>
main branch. All fixes committed. Dockview not rendering on course pages — user reports "lost again" after dev server restart. bootWorkspace (slug.astro ~4403) looks correct but something silently fails.

Vite cache cleared in last session. Restart dev with `npm run dev`.
VPS prod: LiveClassNoteFolder userId migration NOT yet applied.
</current_state>

<completed_work>
- Fix 1 (6bb1730): bootWorkspace reads courseId from [data-course-layout-root] data-live-course-id
- Fix 2 (6bb1730): 70 CONCEPTS subfolder grouping for cursos collection public/*/ entries
- Fix 3 (6bb1730): Search.astro added to slug.astro
- Fix 4 (6bb1730): Camera icon enabled for teachers via canManageLiveInteractions
- Fix 5 (374837e): LiveClassNoteFolder.userId integer→uuid + applied to dev DB
- Fix 6 (374837e): Content-collection entries navigate normally; strip courseId prefix before openNote
</completed_work>

<remaining_work>
- IMMEDIATE: Debug dockview — add console.log to bootWorkspace, find which guard fires
- Restore NOTAS pod functionality (blocked on dockview)
- Apply LiveClassNoteFolder userId migration on VPS prod (human action)
</remaining_work>

<blockers>
- Dockview not rendering. Failure point unknown. Need browser console diagnostics.
</blockers>

## Required Reading
1. `src/pages/[...slug].astro` lines 4399–4416 — bootWorkspace function
2. `src/scripts/course/dockview-workspace.ts` lines 547–580 — initDockviewWorkspace

## Infrastructure State
- Dev DB: userId uuid ✓
- Vite cache: cleared, run `npm run dev`
- VPS prod DB: userId still integer ✗

<context>
Hypotheses (most likely first):
1. initDockviewWorkspace throws — DockviewComponent chunk got 504 on first load after cache clear. classContent never hidden.
2. canManageLiveInteractions false at SSR — entire script block absent from HTML.
3. CSS: container has no height at init time.

Diagnostic: add to bootWorkspace before guards:
  console.log('[dockview] boot: container=', !!container, 'courseId=', courseId);
Wrap initDockviewWorkspace in try/catch to surface errors.
</context>

<next_action>
1. Add console.log diagnostics to bootWorkspace (~line 4403 in [...slug].astro)
2. Open browser devtools → Console, navigate to /s123
3. Read output → find which guard/error stops initialization
4. Fix only that specific failure
</next_action>
