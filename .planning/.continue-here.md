---
context: default
phase: none — iterative course-page UI work
status: in_progress
last_updated: "2026-05-23T09:16:51.471Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

<current_state>
Iterative fixes to the course page ribbon, media pods, and notes routing.
Changes uncommitted this session — commit them first on resume.

The `musiki:open-notas` event on the course page now correctly routes ONLY to
`dockview-workspace.ts`. CoursePodWorkspace.astro no longer intercepts it.

Next task: implement the LiveClassNote → pod migration so the Notes ribbon button
opens something useful (a pod that shows the user's LiveClassNotes from the DB).
</current_state>

<completed_work>

**This session (2026-05-23):**

1. **Media pods — drag + multi-pod + media types** (`src/scripts/course/dockview-workspace.ts`)
   - Deleted `buildMediaShell` (bespoke, no drag infra)
   - Added `mountMediaBody(bodyEl, rawUrl)` — renders `<img>`, `<audio>`, `<video>`, or `<iframe>` based on URL extension
   - Media pod in `createComponent` now uses `buildShell(panelId, url, title, dockview)` + hides pencil btn → full drag/drop support
   - `openMedia` positioning: first media → below last panel; subsequent media → right of last media pod (tiles horizontally)
   - `toEmbedUrl` extended: Vimeo (`player.vimeo.com/video/ID`), SoundCloud (widget API URL)

2. **Ribbon tablet (501–1024px) fix** (`src/styles/ribbon.css`)
   - Tablet rule moved from slug.astro scoped `<style>` → `ribbon.css` (global) so it reliably reaches `<nav class="ribbon">` from Ribbon.astro
   - Removed duplicate rule from slug.astro

3. **Ribbon mobile (<500px) fix** (`src/styles/ribbon.css`)
   - Padding: `8px 12px calc(8px + env(safe-area-inset-bottom, 0px))` — equal top/bottom margin, buttons clear the edge
   - Icons: 32px/15px → 28px/13px — less crowded in thin bottom bar

4. **Notas button routing fix** (`src/components/course/CoursePodWorkspace.astro`)
   - Removed `window.addEventListener('musiki:open-notas', open)` — was opening the room-style CoursePodWorkspace overlay
   - `dockview-workspace.ts` owns this event on the course page
</completed_work>

<remaining_work>

**Immediate next task — LiveClassNote pod:**
The Notes ribbon button (`musiki:open-notas`) in `dockview-workspace.ts` (line 783) does
nothing useful when 0 panels are open and `initialSlug` is null.

Goal: open a pod that shows the user's LiveClassNotes (stored in `LiveClassNote` DB table).
The full page version lives at `src/pages/notas.astro`.

Steps:
1. Create `/api/live/notes` GET endpoint returning current user's LiveClassNotes as JSON
2. Add `'live-notes'` panel kind to `dockview-workspace.ts` (list + viewer in the pod body)
3. Wire `musiki:open-notas` to open this panel when no file-tree notes are open

Key files to read first:
- `src/pages/notas.astro` — DB query, grouping logic, HTML structure
- `src/pages/notas-editor.astro` — editor component
- `src/scripts/course/dockview-workspace.ts` line 783 — handler to update

**Other open items (from previous sessions):**
- Deploy to Hetzner: `git pull && npm run build && docker restart devmusiki-app`
- Info sidebar panel content (right sidebar)
- CoursePodWorkspace.astro still mounted in slug.astro (line 13263) — evaluate removal
- `musiki:open-notas` with 0 panels and no initialSlug: currently no-op
</remaining_work>

<decisions_made>

- Media pods reuse `buildShell` for the outer wrapper (drag infra) instead of a bespoke div tree. Pencil button hidden for media pods.
- First media pod goes `below` last panel; subsequent ones go `right` of last media pod (tiles horizontally, not vertically).
- Tablet ribbon rule belongs in `ribbon.css`, not slug.astro scoped style — Astro scope hash propagation to child components is fragile across refactors.
- `CoursePodWorkspace.astro` is NOT removed from slug.astro yet — only the `musiki:open-notas` listener was cut.
</decisions_made>

<blockers>

- **LiveClassNote pod**: Requires client-fetchable API — `notas.astro` does DB query server-side at render time. Need `/api/live/notes` GET endpoint.
</blockers>

## Required Reading (in order)
1. `src/pages/notas.astro` — understand data fetching and grouping logic
2. `src/scripts/course/dockview-workspace.ts` — existing pod types and `openMedia`/`openNote` API
3. `src/components/course/CoursePodWorkspace.astro` — current state (listener removed)

## Infrastructure State
- Dev server: not running
- DB: production Hetzner VPS — see memory reference_vps.md
- All session changes UNCOMMITTED (commit first on resume)

<context>
Course page has two note systems:
1. **Course file-tree notes** (`dockview-workspace.ts`) — markdown files in the course repo,
   opened by clicking sidebar items. Already works well.
2. **LiveClassNotes** (`notas.astro`) — personal notes written during live class, stored in
   `LiveClassNote` DB table (fields: title, body, renderedHtml, noteDate, courseId, roomName).
   Currently accessible only at /notas standalone page. Should also be a pod.

The Notes ribbon button should open LiveClassNotes pod (case 2), not the file-tree workspace.
</context>

<next_action>
1. Commit all uncommitted changes: ribbon.css, dockview-workspace.ts, CoursePodWorkspace.astro, slug.astro
2. Read src/pages/notas.astro in full
3. Design and create /api/live/notes GET endpoint
4. Add live-notes pod kind to dockview-workspace.ts
5. Wire musiki:open-notas to open it
</next_action>
