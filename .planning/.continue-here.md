---
context: default
phase: null
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-05-21T17:45:47Z
---

# Dynamic Notes Sidebar — Paused at Task 4/5

No blocking constraints. Safe to resume immediately.

<current_state>
Implementing Dynamic Notes Sidebar (System 1 of 2 from 2026-05-21-sidebar-dockview-notes-design.md).

The complete `notes-sidebar.ts` module is built and committed (Tasks 1–3). What remains is wiring it into the Astro course page (Task 4) and a small inline-editor fix + verification (Task 5).

Using superpowers:subagent-driven-development throughout.
</current_state>

<completed_work>
- Task 1 ✅ — `src/scripts/course/sidebar/notes-sidebar.ts` utilities (groupByChapter, computeNewOrders, noteSlugToRelPath, slugify, escHtml) + test file + .mjs mirror + package.json glob (commit 0808c00)
- Task 2 ✅ — `renderNotesSidebar` with chapter HTML (existing CSS classes), context menus (note: Editar/Renombrar slug/Eliminar/Nueva nota aquí; chapter: Nueva nota/Renombrar/Nuevo subcapítulo), DnD drop lines + cross-chapter drop (commit 8c9d281)
- Task 3 ✅ — `initNotesSidebar(container, courseId, courseHref, activeSlug)` with CSS injection, refresh/note-open event listeners (commit 599a37f)
</completed_work>

<remaining_work>
- **Task 4** (NOT started): Wire `[data-notes-sidebar]` into `src/pages/[...slug].astro`:
  1. Replace teacher `sidebarChapterEntries.map(...)` with `<div data-notes-sidebar data-course-id=... data-course-href={courseHref} data-active-slug=...>`; keep recursos section static
  2. Remove `course-teacher-actions` div (E/C buttons, ~lines 3418–3438)
  3. Add init `<script>` importing `initNotesSidebar`, reads container dataset
  4. Non-teacher branch: original sidebarChapterEntries.map() UNCHANGED

- **Task 5** (NOT started):
  1. Add `window.dispatchEvent(new CustomEvent('notes-sidebar-refresh'))` to `saveCurrentNote` in `src/scripts/course/notes/inline-editor.ts`
  2. Manual DnD verification step (verify YAML updated on disk)
</remaining_work>

<decisions_made>
- `noteSlugToRelPath` returns just the path segment; caller prepends `courseHref` (avoids courseId ≠ URL-path mismatch)
- `is-active-lesson` class (not `active`) to avoid conflict with Astro static rendering
- `moveNote`/`deleteNote` receive bare slugs (strip `cursos/${courseId}/` + `.md`); server adds them back
- All async mutation handlers wrapped in try-catch with console.error
- `data-course-href={courseHref}` must be on the div (not just courseId) — courseHref is pre-computed by Astro and always correct
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `docs/superpowers/plans/2026-05-21-plan1-dynamic-notes-sidebar.md` Task 4 section — exact Astro edit steps
2. `src/scripts/course/sidebar/notes-sidebar.ts` — the module being wired in
3. `src/pages/[...slug].astro` lines 3410–3480 — sidebar section being modified

## Infrastructure State

- All changes committed to `main` (5 commits this session)
- `package.json` test script updated to include sidebar tests
- Dev server: not running

<context>
superpowers:subagent-driven-development — implementer (sonnet for complex, haiku for mechanical) → spec reviewer → code quality reviewer per task. Fix issues, re-review, mark complete.

Task 4 is a surgical edit to a 12k-line Astro file. Key correctness requirements:
1. `data-course-href={courseHref}` on the div (courseHref is already computed at line 170)
2. Keep recursos section static even for teachers — only notes chapters are replaced
3. Non-teacher branch is original sidebarChapterEntries.map() with ZERO modifications
4. Remove only the `course-teacher-actions` div (lines ~3418–3438), not surrounding buttons
</context>

<next_action>
Load `docs/superpowers/plans/2026-05-21-plan1-dynamic-notes-sidebar.md` Task 4 section. Invoke superpowers:subagent-driven-development. Dispatch sonnet implementer for Task 4 (complex Astro edit). Then spec + quality review. Then Task 5.
</next_action>
