---
context: deliberation
phase: notes-editor-redesign
task: brainstorm-design
status: in_progress
last_updated: 2026-05-20T18:05:51Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints — diseño validado, nada de código escrito aún._

<current_state>
Brainstorming session completada para rediseño del sistema de notas de cursos (drop GitHub, editor web directo).
Diseño visual aprobado por el usuario. Falta: 1-2 preguntas finales + escribir spec formal + plan de implementación.

Visual companion server (no activo). Mockups guardados en:
`.superpowers/brainstorm/67816-1779298244/content/`
El último mockup aprobado: `editor-layout-v3.html`
</current_state>

<completed_work>
## Diseño completo validado

### Arquitectura
- **Storage**: Filesystem VPS como source of truth (src/content/cursos/). Sin GitHub App write path.
- Alineado con `docs/architecture/class-workspace-refactor.md` — Postgres como canónico a largo plazo
- Repos activos: `../s123` y `../i1` (parent del framework, no en git del framework)

### Scope del branch: Fase 1 + Fase 2 (no Postgres migration aún)
- **Fase 1**: API REST directa al filesystem (`/api/notes/save`, `/api/notes/create`, `/api/notes/delete`, `/api/notes/move`)
- **Fase 2**: Página `/notas-editor?course=[id]` con tree + editor UI

### Layout aprobado (editor-layout-v3.html)
- Split: árbol izquierdo + editor derecho. Página dedicada solo teachers.
- Árbol organizado por `chapter:` YAML field, drag-drop entre chapters
- Menú contextual click-derecho: Editar / Duplicar / Mover / Cambiar order / Borrar
- Reutiliza `src/scripts/room/recursos/filetree.ts` (tiene drag-drop + context menu)

### YAML Strip (lineal, fila única)
- `type ▾` | `chapter ▾` | `status ▾` | `order` | `theme ▾` + Descartar | 💾 Guardar
- Dropdowns con valores sugeridos de notas existentes del mismo curso

### Fila EVAL contextual
- SOLO aparece cuando cursor está dentro de bloque ` ```eval ``` `
- Edita proto-YAML interior del bloque en tiempo real
- Borde izquierdo naranja para distinguir visualmente
- Campos: `evalType ▾` | `dueDate` | `maxScore` | `questions: N ▸`
- PENDIENTE: confirmar campos específicos por evalType

### Toolbar de snippets
`🎴 COVER` | `🎵 LILY` | `📊 MERMAID` | `🖼️ IFRAME` | `🖼️ EVAL` | `📷 IMG (S3)`

### Drop behaviors
- Image drop/paste → S3 upload → inserta `![](url)` (compatible Obsidian, mismo flujo que Foro)
- YouTube/Vimeo URL drop → `<iframe>`
- Cualquier URL → `[título](url)`

### Carpeta EVALS (teacher-only)
- Sección especial en árbol, visible solo teachers
- Templates: `_template-patch-ai`, `_template-class-reveal`, etc. + guías
- Menú contextual propio: Nuevo template / Nueva guía / Usar como base
</completed_work>

<remaining_work>
1. **1-2 preguntas finales de brainstorming**:
   - ¿Qué campos tiene un eval `class-reveal`? ¿Y `patch-ai`? (para la fila EVAL contextual)
   - ¿El editor usa textarea simple o CodeMirror/Monaco? (afecta detección de cursor en bloque)

2. **Escribir spec** → `docs/superpowers/specs/2026-05-20-notes-editor-redesign.md`

3. **Invocar `superpowers:writing-plans`** → plan de implementación

4. **Crear branch local** y empezar:
   - `src/pages/api/notes/` — nuevas API routes
   - `src/pages/notas-editor.astro` — nueva página
</remaining_work>

<decisions_made>
- Drop GitHub App para writes — editor escribe directo al VPS filesystem
- Storage = filesystem (no Postgres migration en este branch)
- Layout = split page dedicada (A)
- Tree reutilizado de filetree.ts (folder → chapter mapping)
- Image upload = igual que Foro (S3, markdown compatible Obsidian)
- Scope = Fase 1+2 únicamente
- Obsidian = import/export manual, no write path primario
</decisions_made>

<blockers>
Ninguno. Listo para spec + plan.
</blockers>

## Required Reading (en orden)
1. `.planning/.continue-here.md` — este archivo
2. `docs/architecture/class-workspace-refactor.md` — arquitectura Postgres canónica
3. `docs/notes-types.md` — tipos y visibilidad de notas
4. `src/scripts/room/recursos/filetree.ts` — componente tree a reutilizar
5. `src/pages/api/content-admin/publish.ts` — API actual (referencia para nueva)
6. `src/pages/editor.astro` — editor actual (referencia)
7. `.superpowers/brainstorm/67816-1779298244/content/editor-layout-v3.html` — mockup final

## Infrastructure State
- VPS: `46.225.154.68` — SSH `zz@` (cambios de usuario) o `root@` (cambios de sistema)
- Content activo VPS: `/opt/musiki/framework/src/content/cursos/i1/` y `.../s123/`
- Repos locales: `../i1` y `../s123` (parent del framework)
- Branch a crear: `feature/notes-editor-direct-write` (sugerido)

## Pre-Execution Critique Required
- Design artifact: `.superpowers/brainstorm/67816-1779298244/content/editor-layout-v3.html`
- Critique focus: ¿La detección de cursor dentro de un bloque eval requiere CodeMirror, o es viable con textarea + selectionStart?
- Gate: Responder esta pregunta antes de escribir el plan de implementación

<context>
Sesión muy productiva. Usuario quiere reemplazar flujo Obsidian→GitHub→build por editor web que escribe directo al VPS. Diseño visual aprobado en tres iteraciones en el browser. El usuario es el único teacher activo (i1 y s123). Stilo de comunicación: directo, técnico, hispanohablante.

NOTA: El archivo anterior .continue-here.md tenía estado de SA/SV (mayo 3) — ese trabajo ya fue mergeado a main. Esto reemplaza ese contexto.
</context>

<next_action>
Start with: Nueva sesión → leer este archivo → completar 1-2 preguntas de brainstorming (eval fields + editor engine) → escribir spec en docs/superpowers/specs/ → invocar superpowers:writing-plans.
</next_action>
