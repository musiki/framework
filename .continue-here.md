---
context: default
phase: notas-workspace-qa-pod
task: 1
total_tasks: 10
status: in_progress
last_updated: 2026-05-23T13:11:24Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

<current_state>
Brainstorming and planning complete. Zero implementation done — all 10 tasks are not_started.

**Execution method chosen:** Subagent-driven development (superpowers:subagent-driven-development).
Fresh subagent per task + two-stage review (spec compliance first, then code quality).

Session ended at 73% context before implementation began.
</current_state>

<completed_work>
- Design spec written and committed: `docs/superpowers/specs/2026-05-23-notas-workspace-qa-pod-design.md`
- Implementation plan written and committed: `docs/superpowers/plans/2026-05-23-notas-workspace-qa-pod.md`
- No code written yet.
</completed_work>

<remaining_work>
All 10 tasks from the plan — execute in order (strict dependencies):

1. DB migration — `folderId` column on `LiveClassNote` + extend `/api/live/notes` filters
2. New `/api/note-folders` CRUD endpoint
3. Extract `buildShell` + `injectWorkspaceCss` to `src/scripts/course/dockview-shell.ts`
4. QA analyzer pure logic (`qa-analyzer-logic.ts`) + unit tests (`.test.mjs`)
5. `db-note` + `qa-analyzer` panel kinds in `dockview-workspace.ts` + HUD + remove `musiki:open-notas` handler
6. NOTAS sidebar section in `[...slug].astro` + `notes-sidebar.ts`
7. `PersonalNotesOverlay.astro` + `personal-notes-workspace.ts`
8. Room workspace — register `db-note` in `RoomWorkspaceManager` + `PodTemplates.astro`
9. Remove `/notas` page → redirect to `/dashboard`
10. `package.json` test update + full integration walkthrough
</remaining_work>

<decisions_made>
- **Approach B chosen**: separate `personal-notes-workspace.ts` for ribbon overlay; course dockview + room both gain `db-note` pod type but keep own lifecycles. DB tree is the shared contract.
- **Course is invisible meta-container**: never shown as a label in NOTAS UI (cognitive economy)
- **No pre-fixed folders**: fully user-created folder structure
- **db-note edit mode = textarea MVP**: CodeMirror can be added later (YAGNI)
- **QA pod receives notes via**: `musiki:send-to-qa` event OR drag `text/x-musiki-note` onto pod
- **`musiki:open-notas` moves**: removed from `dockview-workspace.ts`, handled by `PersonalNotesOverlay.astro`
- **Writing HUD**: `font-size:.62rem`, `opacity:.4`, 1 line: `N palabras · N caracteres · N oraciones [QA ↗]`
- **`renderKwic` window cache**: `(window as any).__qaLogic` must be set after dynamic import resolves in `activateQaNote`
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `docs/superpowers/plans/2026-05-23-notas-workspace-qa-pod.md` — full 10-task implementation plan
2. `docs/superpowers/specs/2026-05-23-notas-workspace-qa-pod-design.md` — design spec

## Key Files (for context)
- `src/scripts/course/dockview-workspace.ts` — course dockview (Tasks 3, 5)
- `src/pages/api/live/notes.ts` — notes API (Task 1)
- `src/scripts/room/workspace/RoomWorkspaceManager.ts` — room workspace (Task 8)
- `src/components/room/workspace/PodTemplates.astro` — pod templates (Task 8)
- `src/pages/[...slug].astro` — course page (Tasks 6, 7)
- `src/components/Ribbon.astro` — ribbon (already fires `musiki:open-notas`)

## Infrastructure State
- Dev server: not running (start with `npm run dev`)
- DB: PostgreSQL on Docker, connect via `psql -h localhost -p 5433 -U musiki -d musiki26`
- Test runner: `npm test` or `node --test "src/scripts/notas/*.test.mjs"`

<context>
This is the NOTAS workspace + QA pod feature. Full QDA (qualitative data analysis) pipeline for musiki LMS. Personal notes become dockview pods accessible from course sidebar, ribbon overlay, and live room. First QA tool: word frequency + KWIC concordance (pure client-side, no dependencies).

The subagent-driven approach was chosen. Each task gets a fresh subagent + two-stage review (spec compliance → code quality). Do NOT run tasks in parallel — they have sequential dependencies.
</context>

<next_action>
Start with: Invoke `superpowers:subagent-driven-development` skill, read the plan from `docs/superpowers/plans/2026-05-23-notas-workspace-qa-pod.md`, create TodoWrite for all 10 tasks, then dispatch Task 1 implementer subagent.
</next_action>
