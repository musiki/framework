---
context: default
phase: notas-dockview-db-integration
task: null
total_tasks: null
status: in_progress
last_updated: 2026-05-23T09:20:56Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

<current_state>
Ad-hoc feature work outside GSD phases. The GSD milestone (v1.0) is 100% complete.

We are working on integrating the notas.astro DB-backed notes (LiveClassNote table) into the dockview workspace pod system, and fixing the dead-end `musiki:open-notas` handler.

Two tasks remain unstarted:
1. **DB notes as dockview pods** — LiveClassNote records from DB should open as pods in the dockview workspace (new pod type, not the course file-tree notes path).
2. **open-notas fallback** — the `musiki:open-notas` handler at `dockview-workspace.ts:783` does nothing when no panels are open and `initialSlug` is null. Should open a blank note or a notes-list panel.
</current_state>

<completed_work>
- Ribbon tablet/mobile layout + media pod drag + notas routing (9b9063b)
- CoursePodWorkspace overlay + ribbon/layout fixes (06346ef)
- Revert bad notas fallback (navigation-to-course-home regression) (00d4c93)
- Ribbon layout — tablet gap, mobile safe-area, graph modal (4429070)
- 12 ribbon regressions fixed: course titles, logo +20%, Shift+S search, nav toggles, 501–1024px layout, mob icons (72adc61)
- Vivo cam icon (f71d913)
- YAML frontmatter array stripping fix (e52b044)
</completed_work>

<remaining_work>
1. **musiki:open-notas fallback** (`src/scripts/course/dockview-workspace.ts:783`):
   - Current: if panels open → setActive panel[0]; else if initialSlug → openNote(initialSlug)
   - Missing: when no panels AND no initialSlug → should open blank note OR notes-list panel
   - Decision needed: blank note vs. notes-list overlay

2. **DB notes as dockview pods** (`src/pages/notas.astro` + `dockview-workspace.ts`):
   - notas.astro fetches LiveClassNote records from DB (line 21): `FROM "LiveClassNote" WHERE "userId" = $1`
   - These notes are currently just sidebar list items on notas.astro
   - They should become pods in the dockview workspace (a new note type that loads from DB list, not course file tree)
   - API endpoint exists: `src/pages/api/live/notes.ts` (GET list, POST create, PATCH update, DELETE)
</remaining_work>

<decisions_made>
- notas.astro LiveClassNote records should become dockview pods, consistent with pod-centric workspace model
- CoursePodWorkspace overlay approach chosen for course pages (committed)
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `src/scripts/course/dockview-workspace.ts:783` — the `musiki:open-notas` handler to fix
2. `src/pages/notas.astro` — DB note fetching and sidebar structure
3. `src/pages/api/live/notes.ts` — LiveClassNote CRUD API

## Critical Anti-Patterns (do NOT repeat these)
- **BAD NOTAS FALLBACK**: A previous session added a fallback in notas routing that caused navigation to course home instead of staying in context (reverted in 00d4c93). Any notas routing fallback must NOT redirect to a course page.

## Infrastructure State
- Dev server: not running (start with `npm run dev`)
- DB: PostgreSQL on Docker at 172.18.0.2 (Hetzner VPS via SSH tunnel if needed)
- No Vercel — production is Hetzner VPS (IP 46.225.154.68)

<context>
The workspace pod system is the core abstraction. The dockview workspace handles course file-tree notes already. DB notes (LiveClassNote) are a separate data source that needs wiring into the same pod system. The open-notas handler needs a meaningful default action when called with no context.

Key files:
- `src/scripts/course/dockview-workspace.ts` — workspace manager, openNote(), musiki:open-notas handler
- `src/pages/notas.astro` — standalone notes page with DB-fetched LiveClassNote records
- `src/pages/api/live/notes.ts` — REST API for LiveClassNote CRUD
- `src/components/course/CoursePodWorkspace.astro` — overlay component (recently added)
- `src/components/Ribbon.astro` — fires musiki:open-notas on "Notas" button click
</context>

<next_action>
Start with: Fix `musiki:open-notas` handler in `src/scripts/course/dockview-workspace.ts:783` — add the missing else branch to open a blank note or notes-list panel when no panels are open and initialSlug is null. Then design the DB-backed note pod type for notas.astro.
</next_action>
