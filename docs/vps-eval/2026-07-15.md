---
date: 2026-07-15
cpu_load: "2.07, 3.11, 3.41"
ram_used: 7.9
ram_total: 15
disk_used_pct: 82
disk_free_gb: 26
pm2_active: 0
ollama_ram_gb: 0.0
uptime_days: 65.8
status: "OK"
alerts: "Moodle 5 y MediaWiki 1.46 migrados. Dominios definitivos activos (edu/wiki). Limpieza profunda de logs, legacy y instaladores .whl completada con éxito."
---

# Reporte de VPS - 2026-07-15

## Estado del Sistema

- **Host:** `vps2`
- **CPU:** Load Average `2.07, 3.11, 3.41` (Carga temporalmente alta debido a tareas de compilación, composer y actualizaciones de bases de datos. Se estabilizará rápidamente).
- **RAM:** `15GiB` total | `7.9GiB` usada | `3.2GiB` libre | `7.3GiB` disponible. (Se mantiene extremadamente estable: la migración a Docker de PHP-FPM y MariaDB generó un incremento mínimo de ~300MB de RAM frente a mayo).
- **Swap:** `0B` configurada.
- **Disco:** `/` con `150G` total | `118G` usado | `26G` disponible (`82%` de ocupación). **[MEJORADO: Ocupación segura tras limpieza de 11.7 GB de archivos redundantes]**
- **Puertos activos nuevos:** FastCGI proxy a `127.0.0.1:9000` (Docker PHP-FPM) y `127.0.0.1:3306` (MariaDB).
- **Apps actuales en PM2:** `0` (Ningún proceso de Node corriendo en PM2 actualmente).

## Servicios Críticos

| Servicio | Estado | Nota |
| :--- | :--- | :--- |
| Caddy | Running | Activo y estable. Certificados SSL emitidos para `edu.musiki.org.ar` y `wiki.musiki.org.ar`. |
| PM2 | Running | `pm2-zz.service` activo gestionando procesos del usuario `zz` (actualmente inactivos). |
| Docker devmusiki | **Stopped / Deleted** | Eliminado. |
| Docker Authentik | **Deleted** | Eliminado y limpiado completamente de la infraestructura, liberando recursos y descartando imágenes. |
| Docker logto | Running | Activo en `3101/3102`. |
| Docker musiki-php | Running | PHP 8.3 FPM activo sirviendo Moodle y MediaWiki de forma segura. |
| Docker musiki-db | Running | MariaDB 11.4 sirviendo las bases de datos de Moodle y MediaWiki. |

## Desglose de Limpieza de Disco Realizada

Se completó una limpieza profunda que redujo el uso de disco del **90% al 82%**, liberando un total de **11.7 GB** de archivos del sistema:

*   `/opt/musiki-wiki/musiki.org.ar.tar.gz`: **6.3 GB** (Eliminado. El backup comprimido original de Hostinger ya está totalmente integrado).
*   `/opt/musiki-wiki/public_html/wiki_legacy`: **3.8 GB** (Eliminado. La carpeta de archivos vieja ya no es necesaria tras migrar `images/`).
*   `/opt/soog/backend/*.whl`: **1.43 GB** (Eliminado. Los instaladores wheel de torch y cuda en python ya fueron eliminados, manteniendo el entorno virtual `venv` intacto).
*   `/opt/musiki-wiki/u256367478_musiki.sql.gz`: **207 MB** (Eliminado. Dump SQL importado).
*   *Reclamado en Docker:* Se ejecutó `docker image prune -a` y `docker builder prune`, liberando **1.27 GB** de imágenes huérfanas de Authentik y capas de build cache.

## Análisis de Performance & Recomendación de Migración (Hostinger ➜ VPS)

### ¿Se puede apagar Hostinger ya?
**Sí, absolutamente.** 
- **Rendimiento e Infraestructura:** El VPS es técnicamente muy superior a la instancia de Hostinger. La arquitectura moderna en contenedores aislados (PHP 8.3 FPM + MariaDB 11.4) detrás de Caddy provee un rendimiento extremadamente rápido con excelente manejo de caché (APCu, OPcache).
- **Impacto de Carga:** El consumo adicional de CPU y memoria tras levantar Moodle y MediaWiki es mínimo (~300MB de RAM). El VPS tiene 15 GB de RAM, con más de 7 GB disponibles. No hay riesgo de sobrecarga por carga de usuarios.
- **Espacio en Disco:** Tras la limpieza, el almacenamiento está en un seguro **82% de ocupación (26 GB libres)**, garantizando espacio suficiente para logs y bases de datos.

### Próximos Pasos Recomendados:
1. Monitorear periódicamente los logs de Caddy.
2. Desactivar Hostinger por completo antes de la fecha límite de Octubre.

---
*Trackeo ##2026-07-15: Finalizada la migración y actualización exitosa de Moodle 5.2.1 (edu.musiki.org.ar) y MediaWiki 1.46.0 (wiki.musiki.org.ar) bajo Docker + Caddy con SSL. Se resolvieron compatibilidades de extensiones (REL1_46) y Google OAuth. Se realizó limpieza profunda de 11.7 GB (legacy, backups, .whl y docker cache), reduciendo el disco al 82% y dejando la VPS en estado óptimo (OK).*
