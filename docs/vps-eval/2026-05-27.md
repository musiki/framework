---
date: 2026-05-27
cpu_load: "0.18, 0.20, 0.18"
ram_used: 7.6
ram_total: 15
disk_used_pct: 83
disk_free_gb: 25
pm2_active: 10
ollama_ram_gb: 2.3
uptime_days: 16.7
status: "Warning"
alerts: "Disco al 83% (25GB libres) tras limpieza, sin swap"
---

# Reporte de VPS - 2026-05-27

## Estado del Sistema

- **Host:** `vps2`
- **CPU:** Load Average `0.18, 0.20, 0.18` (Muy estable y con carga mínima).
- **RAM:** `15GiB` total | `7.6GiB` usada | `3.3GiB` libre | `7.7GiB` disponible. (Mejorado tras detener contenedores de desarrollo).
- **Swap:** `0B` configurada (Sigue sin swap activa).
- **Disco:** `/` con `150G` total | `120G` usado | `25G` disponible (`83%` de ocupación). **[MEJORADO: Liberados 13 GB de espacio]**
- **Puertos:** Caddy (`80/443`), SSH (`22`), Ollama (`11434`), LilyPond (`4543`), Musiki/dev (`4321/4322`), Trawun (`4000`), Arpochat (`3000/3001`), LiveKit (`7880/7881`), Fastify (`8787`), Soog (`10000`).
- **Apps actuales en PM2:** 10 servicios en ejecución estable (uptime general de 18 horas).

## Servicios Críticos

| Servicio | Estado | Nota |
| :--- | :--- | :--- |
| Caddy | Running | Activo y estable en `80/443` (uptime: 2 semanas y 2 días). |
| PM2 | Running | `pm2-zz.service` activo gestionando 10 procesos online. |
| Ollama | Running | Activo localmente en `11434`. Consumo RAM actual: `2.3GiB` (pico `9.6GiB`). |
| GitHub Runner (`musiki`) | Running | **Corregido.** `actions.runner.musiki-framework.musiki-vps-runner.service` ahora está `active` y corriendo bajo `systemd` (sin SessionConflictException). |
| GitHub Runner (`trawun`) | Running | `actions.runner.zzigo-trawun.trawun-vps-runner.service` `active` y corriendo bajo `systemd`. |
| Docker devmusiki | **Stopped / Deleted** | Se detuvieron y removieron por completo los contenedores `devmusiki-app`, `devmusiki-server`, `devmusiki-db` y el volumen `devmusiki-postgres-data`. |
| Docker Authentik | Running | Los contenedores productivos de Authentik continúan activos y saludables. |

## Desglose de Uso de Disco (Hogs)

Tras la limpieza profunda del disco (eliminación de `/opt/soog/backend/venv_old`, `/opt/openclaw`, `/opt/devmusiki` e imágenes docker obsoletas), el almacenamiento se distribuye de la siguiente manera:

*   `/opt/soog/backend`: **36.5 GB** (Reducido de 45 GB tras eliminar `venv_old`)
    *   `/models`: `17 GB` (modelos de IA)
    *   `/.cache`: `13 GB` (caché de HuggingFace/Pip)
    *   `/venv`: `5.9 GB` (entorno virtual activo)
*   `/home/zz/`: **13 GB**
    *   `/.local/share/pnpm`: `1.9 GB` (caché de pnpm)
    *   `/.local/share/nvm`: `1.1 GB` (versiones de Node)
    *   `/actions-runner`: `2.4 GB`
    *   `/actions-runner-trawun`: `2.2 GB`
    *   `/.rustup`: `1.4 GB`
*   `/opt/legacy-musiki`: **10 GB**
    *   `musiki.org.ar.tar.gz`: `6.3 GB` (backup del sitio viejo - **Candidato a descargar y eliminar**)
    *   `/public_html`: `3.8 GB` (archivos del sitio viejo)
*   `/opt/edu.musiki.org.ar`: **3.0 GB**
*   `/opt/openclaw`: **0 GB** (Eliminado por completo)
*   `/opt/devmusiki`: **0 GB** (Eliminado por completo)
*   `/opt/musiki`: **2.3 GB**

## Análisis de Performance

### PROS
- **Liberación de Almacenamiento**: Pasamos de un crítico 92% a un 83% de ocupación, abriendo un margen de seguridad de `25 GB` de disco libre.
- **Limpieza de Recursos Ociosos**: Detener los contenedores docker de desarrollo de `devmusiki` ayuda a mantener la VPS libre de procesos huérfanas.
- **Estabilidad de los Runners**: Se mantiene el correcto funcionamiento de los runners unificados de `musiki-framework` y `trawun` bajo systemd.

### CONS (Mejoras)
- **Ocupación de Disco Aún Alta (83%)**: Si bien ya no es crítica, se recomienda monitorear. El siguiente paso de optimización es descargar localmente el backup de `6.3 GB` de `/opt/legacy-musiki` y eliminarlo de la VPS.
- **Sin swap**: Sigue sin haber swap activa.

---
*Trackeo ##2026-05-27: Realizada limpieza profunda de disco (venv_old, openclaw, devmusiki docker/archivos) liberando 13 GB y reduciendo ocupación al 83%. Runners estables bajo systemd y servicios productivos saludables.*
